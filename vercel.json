{
  "rewrites": [
    { "source": "/:brandSlug/:pageSlug", "destination": "/api/pages/:brandSlug/:pageSlug" }
  ],
  "build": {
    "env": {
      "NODE_VERSION": "18"
    }
  },
  "installCommand": "echo Skip install on Vercel (no build step)",
  "buildCommand": "echo Skip build (static + serverless)",
  "outputDirectory": "."
}
